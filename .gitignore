# Generated by `xcodegen generate` from project.yml — not source of truth.
Postcards.xcodeproj/

# Vendored from the dotpostcard repo's `make xcframework` — not source of truth.
Frameworks/

# Xcode user state / build products.
xcuserdata/
DerivedData/
build/
*.xcuserstate
.DS_Store
Postcards/Support/

# XcodeGen-generated Info.plists and entitlements (declared inline in project.yml,
# same as Postcards/Support above — not source of truth). The .swift files alongside
# them in these directories ARE source and stay tracked.
Extensions/**/Info.plist
Extensions/**/*.entitlements
WatchApp/*.entitlements
